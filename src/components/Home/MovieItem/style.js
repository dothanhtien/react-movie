import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => {
  return {
    title: {
      minHeight: 64,
    },
    description: {
      display: "block",
      minHeight: 60.5,
    },
  };
});

export default useStyles;
